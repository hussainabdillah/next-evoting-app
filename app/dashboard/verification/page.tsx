import { VerificationViewPage } from '@/sections/verification/view';

export const metadata = {
  title: 'Evoting : Verification',
};

export default function page() {
  return <VerificationViewPage />;
}